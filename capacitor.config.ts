import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appthamizhi.app',
  appName: 'ApptThamizhi',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
